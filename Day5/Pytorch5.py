# 卷积神经网络
import torch
import torch.nn as nn
import os
# layer = nn.Conv2d(1, 3, kernel_size=3, stride=1, padding=0)
# x = torch.rand(1, 1, 28, 28)
#
# out = layer.forward(x)
# print('out:', out.size())
#
# x = torch.rand(100, 16, 784)
# layer = nn.BatchNorm1d(16)
# out = layer(x)
# print('layer:', layer.running_mean)
print(os.path.join('root', 'name'))